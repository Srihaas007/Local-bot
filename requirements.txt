# Core runtime deps (lightweight)
click>=8.1
pydantic>=2.7
rich>=13.7
python-dotenv>=1.0

# Testing
pytest>=8.2

# Optional providers (install manually as needed)
# llama-cpp-python  # For GGUF models via llama.cpp (CPU/GPU)
# transformers       # For HF models (requires torch)
# torch              # Pick the right CUDA build for your GPU
# bitsandbytes       # Optional 4/8-bit quantization for Transformers

# Optional web UI (FastAPI server and streaming)
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
requests>=2.32

# Optional utilities
# huggingface_hub    # For /model/download with HF repos
